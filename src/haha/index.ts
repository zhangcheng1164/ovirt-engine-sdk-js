export function add(n: number, m: number): number {
  if (n > 1) {
    console.log('--haha---')
    return n + m + 1
  }
  return n + m
}

export function substract(n: number, m: number): number {
  return n - m
}
